% Reduce page margins:
\usepackage[
    top=0.5in,
    bottom=0.5in,
    left=0.5in,
    right=0.5in,
    footskip=0pt, % Eliminates the space reserved for the footer
    nofoot % Ensures that no footer is placed at the bottom of the page.
]{geometry}
\pagenumbering{gobble}  % This disables page numbering

% Adjust line spacing
\usepackage{setspace}
\setstretch{1.0} 

% % Adjust font size
% \usepackage{anyfontsize}
% \renewcommand{\normalsize}{\fontsize{10pt}{12pt}\selectfont}
% \renewcommand{\small}{\fontsize{9pt}{11pt}\selectfont}
% \renewcommand{\footnotesize}{\fontsize{8pt}{10pt}\selectfont}

% % Adjust section spacing
% \usepackage{titlesec}
% \titlespacing*{\section}{0pt}{0.5\baselineskip}{0.3\baselineskip}
% \titlespacing*{\subsection}{0pt}{0.3\baselineskip}{0.2\baselineskip}

% % Use compact lists
% \usepackage{enumitem}
% \setlist{noitemsep, topsep=0pt, parsep=0pt, partopsep=0pt}

% Adjust header and footer
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\thepage}
\setlength{\headheight}{12pt}
\setlength{\headsep}{0.1in}
\setlength{\footskip}{0.1in}